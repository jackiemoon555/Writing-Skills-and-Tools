---
name: project-task-force-cryptid
description: CURRENT book since 2026-09-13 (The Champ shelved) — discovery draft, master Doc "Tbd D1 Take 2", phone satellite runs in parallel so PULL FIRST and never log a duplicate Pass number
metadata:
  type: project
---

Since 2026-09-13 the live project is the untitled fantasy/horror discovery draft, working handle
**"Task Force Cryptid"** (not a title). The Champ is SHELVED at D2 Ch28 + 4 interludes (~16 pieces
left); he'll come back; don't raise it unless he does. Master = Google Doc **"Tbd D1 Take 2"**
(the old "TBD" Doc is stale). Ledger: `reports/task-force-cryptid.room.md`. Floor: 1,000 per
sitting AND 5,000 per week (Mon–Sun), raise only after three clean weeks; week 1 = 09-14→09-20.

**Why:** he wanted fantasy and a loose, free-flow process; [[feedback-per-book-rules]] applies in
full — nothing ports from The Champ. His standing instructions for this draft: every new batch
gets roomed, passes in sequence, revisions HELD; intent/rulings stay in chat — only room passes
and his explicit notes go in the ledger ([[feedback-ask-first-writing-partnership]]).

**How to apply:** he runs a SATELLITE Claude session on his phone beside the PC session, both
writing to the same repo. On 09-16/17 both logged a "Pass 5" on the same chapter and it cost a
manual reconciliation. So: `git pull` before anything, re-check the ledger's highest Pass number
immediately before appending, and if the other machine already logged those pages, log yours as
`Pass N-PC` plus a reconciliation block — never a second `Pass N`. The PC has no `gh` CLI
(push a branch, squash-merge locally into main at handoff) and Python is `py`
(`PYTHONPATH=src py -m writing_tools <file> --report <out.md>`). Craft classes already named
once (don't re-teach, use active recall per [[feedback-active-recall-revision]]): run-up before
the last line / stop dead after it; dialogue is ahead of description (place goes stock when
nothing moves in it); the camera test (can a camera film it?).

**Standing orders (author, 2026-09-19):** the PC session is PRIMARY — the phone satellite follows
it, not the reverse. Revisions are OPEN mid-draft (supersedes "revisions HELD"); he goes back
when a scene calls him, the room never assigns one; the net-words floor is the guard. Declared:
it's a NOVEL; NO EDITOR until the draft is entirely done (hold it, jump guard); title TBD —
"Task Force Cryptid" is only the filing handle ("cheesy"), never propose titles.
