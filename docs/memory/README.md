# Writing project — mirrored context notes

These are the writing-specific "notes to self" (originally created in another project's memory
namespace) mirrored here so this repo is the **self-contained home** of the writing project.
A session run from this repo's folder can read these + `../SESSION_HANDOFF.md` and have full
context without any external memory. Keep them updated here going forward.
